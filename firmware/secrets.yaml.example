# Copy to firmware/esphome/secrets.yaml (never commit secrets.yaml).

wifi_ssid: "your-ssid"
wifi_password: "your-password"

# Portal (Phase 2 — from ~/Downloads/erivan01-voice.creds.json):
# device_connect_nats_jwt: "eyJ..."
# device_connect_nats_seed: "SUAH..."
